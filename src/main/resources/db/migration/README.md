Flyway migrations live here. The first one arrives with the identity module.

This file only keeps the directory present on the classpath, so that Flyway resolves the
configured location instead of warning that it is missing.
