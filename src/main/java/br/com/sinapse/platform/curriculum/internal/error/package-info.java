/**
 * The failures the curriculum module raises deliberately.
 *
 * <p>Each carries an entry of the shared catalogue and no message of its own, so no
 * identifier and no fragment of a database error can reach a response body.
 */
package br.com.sinapse.platform.curriculum.internal.error;
