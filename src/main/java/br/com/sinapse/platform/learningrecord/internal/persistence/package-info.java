/**
 * Spring Data repository of the study session aggregate.
 *
 * <p>Nothing here deletes. A trigger refuses a delete outright, so a method that offered one
 * would only be a way of turning an invariant into a runtime error.
 */
package br.com.sinapse.platform.learningrecord.internal.persistence;
