/**
 * Web layer shared by every module: the versioned route prefix, the error contract and
 * the diagnostic probe used to prove that both behave as specified.
 */
package br.com.sinapse.platform.shared.web;
