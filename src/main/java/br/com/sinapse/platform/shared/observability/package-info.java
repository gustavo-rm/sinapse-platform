/**
 * Observability (ADR 0009).
 *
 * <p>Structured JSON logs with a correlation identifier per request. No personal data
 * is written to a log: not an e-mail address, not a name, not a date of birth, not the
 * study content of a student.
 */
package br.com.sinapse.platform.shared.observability;
